export enum ServerStateText {
  running = "Running",
  stopped = "Stopped",
  crashed = "Crashed",
}

export enum ServerStateColor {
  running = 0x1bd96a,
  stopped = 0xff496e,
  crashed = 0xff496e,
}
